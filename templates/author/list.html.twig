{{dump(a)}}
<table border="2">
    <tr>
        <td>id </td>
        <td> photo</td>
        <td>username</td>
        <td>email</td>
        <td> nb books</td>
         <td> details</td>
       
    </tr>
    {% for auth in a %}
    <tr>
        
        <td>{{auth.id}}</td>
        <td> <img src="{{auth.picture}}"></td>
        <td>{{auth.username | upper}}</td>
        <td>{{auth.email}}</td>
        <td>{{auth.nb_books}}</td>

<td><a href="{{ path('authorDetails', {'id': auth.id}) }}">Voir les détails de l'auteur</a></td>
    </tr>
    {%else%}
    <tr>
        <td>
        le tableau est vide
        </td>
    </tr>
    {% endfor %}
</table>

nb authors:{{a|length}}
{% set books=0 %}

{%for a in a %}
{% set books=books+a.nb_books %}
{% endfor %}
nb books={{books}}